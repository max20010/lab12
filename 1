#include "stdafx.h"
#include "iostream"
using namespace std;
int PowerA3(int& a, int& b) {
	b = pow(a, 3);
	return 0;
}
int main()
{
	int x;
	for (int i = 0;i < 5;i++) {
		cin >> x;
		PowerA3(x, x);
		cout << "x^3 = " << x << endl;
	}
}
